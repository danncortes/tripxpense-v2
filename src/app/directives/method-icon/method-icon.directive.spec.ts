import { MethodIconDirective } from './method-icon.directive';

describe('MethodIconDirective', () => {
  it('should create an instance', () => {
    const directive = new MethodIconDirective();
    expect(directive).toBeTruthy();
  });
});
