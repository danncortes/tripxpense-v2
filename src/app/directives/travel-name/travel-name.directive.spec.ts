import { TravelNameDirective } from './travel-name.directive';

describe('TravelNameDirective', () => {
  it('should create an instance', () => {
    const directive = new TravelNameDirective();
    expect(directive).toBeTruthy();
  });
});
